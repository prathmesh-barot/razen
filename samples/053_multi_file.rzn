// 053: Multiple functions + fmt usage
use std.fmt

func add(a: i32, b: i32) -> i32 {
    ret a + b
}

func mul(a: i32, b: i32) -> i32 {
    ret a * b
}

pub func main() -> i32 {
    x := add(10, 20)
    y := mul(x, 2)
    fmt.println("computed:")
    ret y
}
