// 012: Enum with explicit values (use if-else, match return path not verified)
enum HttpStatus: i32 {
    Ok = 200,
    NotFound = 404,
}

func is_ok(s: HttpStatus) -> i32 {
    if s == HttpStatus.Ok {
        ret 1
    }
    ret 0
}

pub func main() -> i32 {
    ret is_ok(HttpStatus.NotFound)
}
