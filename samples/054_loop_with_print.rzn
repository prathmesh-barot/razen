// 054: Loop with printed output at end
ext func puts(s: str) -> i32

pub func main() -> i32 {
    mut sum : i32 = 0
    mut i : i32 = 1
    loop i <= 5 {
        sum = sum + i
        i = i + 1
    }
    puts("loop complete")
    ret sum
}
